[project]
name = "demo-remote-mcp-todo"
version = "0.1.0"
description = "Add your description here"
readme = "README.md"
requires-python = ">=3.10"
dependencies = [
    "mcp[cli]>=1.22.0",
    "pydantic>=2.12.4",
]
