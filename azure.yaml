# yaml-language-server: $schema=https://raw.githubusercontent.com/Azure/azure-dev/main/schemas/v1.0/azure.yaml.json

name: demo-remote-mcp-todo

services:
  mcp-container-py:
    project: .
    language: python
    host: containerapp
    docker:
      path: ./Dockerfile
      context: .
